import React from 'react';
import PropTypes from 'prop-types';
import './header.scss';

const Header = () => {
    return (
        <header id="header">

        </header>
    );
};

Header.propTypes = {

};

export default Header;
