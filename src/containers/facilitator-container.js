import { connect } from 'react-redux';
import { Facilitator } from 'components';

const mapStateToProps = (state) => ({

});

const mapDispatchToProps = (dispatch) => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(Facilitator);
